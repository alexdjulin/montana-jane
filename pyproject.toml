[project]
name = "aiai-berlin"
version = "0.1.0"
description = "Agentic media hack — starter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fal-client",
    "openai>=3.13.0",
]

[tool.uv]
package = false
